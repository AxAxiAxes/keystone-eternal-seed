# AXES OS terminology records (planned)

Machine-readable term records are expected to be added here as `*.term.json` files,
validated against `../schema/term-record-v1.schema.json`.

This directory is intentionally initialized as part of terminology-standard
infrastructure, but no term-record files are committed yet in this change set.
