# AXES Tier 1 decision register

**Status:** Internal coordination record; no pending decision is approved by
this document
**Recorded:** 2026-09-11
**Owner:** Founder or named human delegate
**Purpose:** Keep the next founder-controlled decisions, external-action
boundaries, and evidence requirements visible across the AXES early-revenue
and continuity program.

## Operating rule

This register tracks decisions; it does not authorize a price, service,
publication, external account change, deployment, legal engagement, payment,
provider action, financial feature, or claim. An item moves from pending only
when the named human decision and its referenced evidence are recorded in the
appropriate private or repository-controlled record.

## Current decision queue

| Priority | Decision | Current state | Required evidence before resolution | Human-controlled boundary |
| --- | --- | --- | --- | --- |
| P0 | Restore provider-backed production chat, if the founder chooses to operate it | **Fix merged and live-verified stable 2026-09-12.** PR #13 merged (`f6e492d`), redeployed automatically. Post-deploy live checks: `GET /health` returns `200` consistently; `POST /api/axiom` (`action: "chat"`) returns a clean, stable `502 {"error":"AXIOM chat is temporarily unavailable"}` across 5 consecutive attempts with no crash, no dropped connection, and no raw/garbled response — confirming the crash-loop itself is resolved. Root cause (superseding the earlier OpenAI hypothesis): a founder-provided Railway deploy-log screenshot showed `axiom-freedom` crash-looping with `Error [ERR_HTTP_HEADERS_SENT]` at `server.js`, triggered by `AXIOM engine returned HTTP 404` for `/system/gravity-center`, `/automation/readiness`, and `/system/bead-passports`. Source inspection found a genuine, systemic bug: 20 route handlers called `res.writeHead(200, ...)` *before* `await invokeEngine(...)` had resolved (nested inside `res.end(JSON.stringify(await invokeEngine(...)))`); when the awaited call rejected, the `catch` block's own `res.writeHead(...)` threw `ERR_HTTP_HEADERS_SENT` because headers were already sent, and this second throw — inside an async callback passed directly to `http.createServer` — became an unhandled promise rejection that terminates the whole Node process by default (Node 20). All 20 call sites were restructured to `const result = await invokeEngine(...); res.writeHead(200, ...); res.end(JSON.stringify(result));`, matching the file's own already-correct convention used elsewhere. A new regression test drives every affected route against a simulated stale (404-returning) engine and asserts the server responds gracefully and stays alive for subsequent requests, rather than crashing the test process. **Founder later reported 21 Railway crash emails already received**, without a clear sense of whether they predate the fix; since 21 crash-restart cycles across 20 vulnerable routes over the hours before the fix is entirely consistent with (and further corroborates) this root cause, ran an additional live stress test: 40 `/health` checks 3 seconds apart (~2 minutes) with 0 failures, plus 8 rapid chat requests targeting the exact previously-crashing route followed immediately by a health check — all 8 returned a clean `502`, and health stayed `200` throughout. This is fresh evidence the process does not crash post-fix, though it can't rule out crashes for all time; the fully conclusive check (comparing the last crash email's timestamp against the fix's live-deploy time, and confirming no further email arrives) remains a founder step. **Remaining, now-isolated issue:** chat itself is still unavailable — this is no longer a crash/process-stability question, it is specifically `axiom-engine` not returning a successful chat response (stale/absent deployment, and/or an OpenAI-side cause), which requires Railway dashboard access this session does not have | Live `POST https://xiiom.com/api/axiom` (`action: "chat"`) tested 5 times post-deploy, then 8 more times in a targeted stress test: stable `502` throughout, no crash-loop signature; 40/40 `/health` checks over 2 minutes passed. `apps/axiom-freedom/test/axiom-proxy.test.js` (`survives engine failures on every automation status route without crashing the process`) passing locally and in CI | Authorized Railway operator should now focus specifically on `axiom-engine`'s own health: confirm its active-deployment/source-connection status, confirm `OPENAI_API_KEY` is set and the service has been redeployed since, and check its own logs for the exact upstream cause (both `axiom-engine` and `axiom-freedom` log `error.message` on failure, per the earlier logging fix). Also check whether any of the 21 crash emails has a timestamp after this fix's live-deploy time (~2026-09-12 06:18 UTC) — if none do, the crash-loop is confirmed fully closed. Requires authorized external Railway/OpenAI account access; nothing further can be diagnosed or fixed from this repository alone |
| P0 | Keep `xiiom.com` and all Railway services online past the current trial period | **New 2026-09-12; confirmed account-wide, not per-project.** The same "24 days or $4.62 left, upgrade to keep your services online" banner was observed both from the Projects list and from inside `lucid-flow`'s own dashboard view, confirming this is one shared account-level trial balance covering every project and service, not a separate quota per project. Also observed: `axiom-freedom`'s Networking tab reports **"You have hit the custom domain limit for your plan"** — the trial plan cannot add another custom domain (such as `axescontracting.com` or `www.xiiom.com`) without upgrading | Founder-reviewed Railway billing/plan page showing current plan, remaining credit or trial window, the specific date/threshold at which services would be suspended if not upgraded, and the custom-domain limit for each candidate paid plan | This is a billing decision requiring the founder's Railway account access and payment method; it cannot be assessed, authorized, or acted on from this repository. If the trial lapses without action, all services in both projects are at risk of going offline; separately, the custom-domain limit already blocks the pending `axescontracting.com` connection (P2 row below) regardless of the trial timer |
| P0 | Merge PR #1 (`axaxiaxes-axes-directory-data-model` → `axaxiaxes-axiom-monorepo`) to deploy the built AXI agent/automation governance system | **Resolved 2026-09-12.** Founder authorized autonomous merging in-session; PR #1 merged (`e9d4f98`), followed by PR #2 (chat-diagnostics/engine-error-forwarding fix, `9b68843`) and PR #3 (vendor/subscription audit checklist, `a2960e8`). All CI green on each merge. Post-merge live check confirmed `xiiom.com/health` returns `{"status":"ok"}`, `/origin-continuity` renders the expected content, and `/support` still correctly returns 401 without credentials | Founder go-ahead was given for autonomous merging in this session; future merges of this scale should still get an explicit founder go-ahead per this row's original intent | Redeploy occurred automatically via Railway's existing branch integration; no manual Railway/DNS action was taken or required |
| P0 | Determine patent/invention filing status and preservation path | Pending. **2026-09-12:** founder additionally reported a call from a New York-area contact stating someone had applied for a patent under the "AXES Contracting Inc" name, and that all company documents/design portfolio since college were lost. Founder indicated this is "water under the bridge" and did not request further investigation; no patent-registry search or other action was taken, per that direction. See `docs/AXI_ETERNAL_ORIGIN_VALUE_AND_CRISIS_REGISTER.md` and `docs/memory/2026-09-12-data-loss-and-ip-ownership-reports.md` | Founder-reviewed private source/filing inventory, dates, and qualified counsel scope if pursued | Requires founder decision and any professional engagement outside this repository |
| P0 | Decide whether to keep this GitHub repository public or switch it to private | **New 2026-09-12; security/breach check.** No unauthorized-access breach was found: the collaborator list is unchanged (only the `AxAxiAxes` owner account, admin role), GitHub secret scanning is enabled with push protection and shows zero alerts, and the repository has zero forks, stars, or watchers. However, the check found the repository's own **visibility is public**, and has been since its creation on 2026-08-28 — every committed document, including the patent-application draft (`docs/keystone/PATENT_APPLICATION_64_078_819.md`) and personal/business/vendor records, has been visible to anyone on the internet the entire time, which contradicts the "private" framing many documents assume. Also found: Dependabot alerts and code scanning are both disabled (a monitoring gap, not itself a breach), and the default branch (`axaxiaxes-axiom-monorepo`) has no branch-protection rule | Founder decision on visibility; if switching to private, review any already-shared public links and note that GitHub Actions minutes/billing can differ between public and private repos depending on plan | This is a repository-configuration change with real consequences (existing integrations, links already shared, CI minutes); this session has not changed visibility and will not without explicit founder direction |
| P0 | Define URNUR's permitted scope | Pending | Written legal/compliance guidance for the actual proposed jurisdiction and feature scope | Financial, banking, currency, token, marketplace, trading, and collectible-exchange work remains inactive |
| P1 | Activate AXES design/materials consultation candidate `AXES-DMC-001` | Not approved | Completed activation record, scope, owner, price basis, terms, capacity, rights/accessibility review, and low/base/high assumptions | No publication, outreach, paid delivery, or professional-service claim before founder approval |
| P1 | Activate KEYSTONE creator-origin setup candidate `KEYSTONE-COS-001` | Not approved | Completed activation record, private data handling, terms, capacity, legal/privacy review, and low/base/high assumptions | Recordkeeping only; no legal conclusion, filing, enforcement, public registry, payment, or financial service |
| P1 | Run a productivity-tool evaluation | Not measured | Founder-reviewed non-sensitive test with manual baseline, assisted time, correction time, quality decision, and permitted account/data settings | Do not use private material or claim productivity savings before measured review |
| P1 | Advance AXIOM Engine and AXIOM Freedom draft registry records | Draft / unverified | Named human owner, Ux relationship, verified source/credit record, bounded authority scope, data/retention record, security/continuity review, and release decision | No KEYSTONE Origin Unit authority, legal-right conclusion, deployment authorization, or public claim from draft records |
| P1 | Approve a specific source asset for reuse | Not approved | Individual asset-use record with source, rights basis, credit/consent, accessibility, privacy, technical-quality, and human decision | Keep private archives and excluded references private; no provider or public reuse without approval |
| P1 | Follow up on Vigour Creative design-packet receipt; monitor third-party listing/search visibility | **Updated 2026-09-12; not a dispute.** Founder clarified: paid "about 2k" (approximate; exact record lost in a prior crash) for a design packet, requested the receipt and content but has not received it in about a year, and the agency reported health issues as the reason. Founder plans to ask directly and already considers their own logos/art direction better than what the agency would have delivered, so there is no creative urgency. Separately, founder reported an older website vanished from search and the business was removed from Angi/Yelp/Bing; independent web checks were inconclusive (mixed signal on Angi, nothing found for Yelp/Bing) — only the founder's own logins to each platform can confirm current status. One evidence-backed contributor: `axescontracting.com`'s already-documented expired HTTPS certificate can independently suppress search ranking/indexing. See `VENDOR_AND_SUBSCRIPTION_AUDIT.md` for full detail | Founder-held contract/receipt once received; founder's own platform-dashboard checks (Angi Pro, Yelp for Business, Bing Places) | Not a technical or repository matter; no legal, financial, or delisting-cause conclusion is offered — any follow-up is the founder's alone |
| P2 | Connect AXES Contracting domain and complete email migration | Pending | Founder-approved access inventory, rollback plan, exact provider records, and post-change verification. **Verified 2026-09-11:** `axescontracting.com`/`www.axescontracting.com` still resolve via `ns1.siteground.net` (legacy SiteGround), not Railway; HTTPS fails with an expired certificate; plain HTTP returns stale legacy content, not the `axiom-web` portal. See `RAILWAY_DEPLOYMENT.md` "Current deployment state." The destination once connected is now a **private, admin-gated command center** (root requires `ADMIN_PASSWORD`), not a public hub page. **Update 2026-09-12:** the `axiom-freedom` service's Networking tab reports the account has already hit its custom-domain limit (`xiiom.com` occupies the one slot available on the current plan), so this connection is also blocked on a plan upgrade, independent of the DNS/registrar work | Requires authorized Railway, DNS, and mail-provider action; do not change settings from this repository |
| P2 | Prepare AXES OS backup/restore phase | Planned | Founder-approved private encrypted backup destination, retention policy, checkpoint transfer, and documented restore drill | No private data transfer or infrastructure procurement without explicit approval |
| P2 | Confirm Railway redeployed `xiiom.com`'s `axiom-web` service after PR #6 merged (`56d71d1`) | **Fully resolved 2026-09-12.** Root cause was a missing `COPY` line for `design-desk.html` in both Dockerfiles (see this row's prior entry); fixed and merged (PR #16, `d3fca4e`). Railway auto-redeployed automatically; live re-check ~90 seconds after merge confirmed `GET https://xiiom.com/design-desk` returns `200` consistently across 5 attempts, with the expected page body (`Architectural Design Desk`, `under internal review`). `/axescontracting` also remains `200` | Live `GET https://xiiom.com/design-desk`: `200` × 5, correct body content, post-merge | None remaining for this item; no further Railway/DNS action needed |
| P2 | Review whether the `handsome-motivation` Railway project is needed, or is unused capacity drawing down the shared trial balance | Pending | **Confirmed 2026-09-12** that `lucid-flow` (not `handsome-motivation`) is the real production project (see P0 chat row). `handsome-motivation`'s `spirited-victory` service runs the same `apps/axiom-engine` code but with a public domain exposed (a deviation from this repository's documented "never create a public domain for axiom-engine" rule); its other service, `nodejs`, is tied to an unrelated, disconnected source repo (`alphasecio/nodejs`), consistent with leftover/template clutter rather than an intentional AXES deployment | Founder must confirm in the Railway dashboard whether `handsome-motivation` is intentionally used for anything (e.g., staging) before deleting it or its services; do not assume it is safe to remove without that confirmation, and no deletion can be performed from this repository |

## Evidence and review cadence

At least weekly, the founder or named human delegate reviews this queue with
the project timeline, budget, evidence ledger, asset-use register, and
application draft records. A review may continue, pause, retire, or reorder a
candidate, but must not mark an item complete without its required evidence.

For income candidates, use `AXES_TIER_1_EVIDENCE_LEDGER.md`; actual revenue,
cost, client, and payment records remain private. For productivity, record
only measured results. For assets, use `AXES_ASSET_USE_READINESS.md`; private
archive contents remain excluded from the repository.

## Related records

- `AXES_BUILD_PROGRAM.md` (this register is the foundation-depth gauge its
  "Foundation depth principle" section checks before each workstream starts)
- `PROJECT_TIMELINE.md`
- `FOUNDER_REVENUE_PRIORITY_OVERLAY.md`
- `KEYSTONE_TIER_1_AUTOMATION_AND_INCOME_PLAN.md`
- `AXES_TIER_1_EVIDENCE_LEDGER.md`
- `AXES_DESIGN_MATERIALS_CONSULTATION_READINESS.md`
- `KEYSTONE_CREATOR_ORIGIN_SETUP_READINESS.md`
- `AXES_ASSET_USE_READINESS.md`
- `keystone/AXIOM_APPLICATION_DRAFT_RECORDS.md`
- `PROJECT_BUDGET.md`
